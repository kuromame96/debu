includes DebuMot;
p=$pat_debu+0;
firstX = $screenWidth * 0.5;
firstY = $screenHeight * 0.1;
frame = 0;
parallel( "sound", $se_ha );

while( true ){
    if( frame % 6 === 0 ) {
        new HanatuKuimono{ firstPosX=x, firstPosY=y };
        $tabeta--;
    };
    
    pUpdate();
    
    frame++;
    update();
}